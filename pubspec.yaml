name: testenovo
description: "Modelo do novo SDK"

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^11.1.0
  cupertino_icons: ^1.0.9
  flutter_bloc: ^9.1.1
  get_it: ^9.2.1
  json_annotation: ^4.12.0
  dio: ^5.9.2
  meta: ^1.17.0
  freezed: ^3.2.5
  freezed_annotation: ^3.1.0
  flutter_ai_toolkit: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  auto_route_generator: ^10.5.0
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true
